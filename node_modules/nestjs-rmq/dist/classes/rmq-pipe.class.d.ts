import { Message } from 'amqplib';
import { LoggerService } from '@nestjs/common';
export declare class RMQPipeClass {
    protected logger: LoggerService;
    constructor(logger?: LoggerService);
    transform(msg: Message): Promise<Message>;
}
